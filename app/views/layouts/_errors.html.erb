<% if @pizza.errors.any? %>
  <div class = "fields_with_errors">
    <h2><%= pluralize(@pizza.errors.count, "error") %> prohibited this pizza from being created:</font></h2>
 
    <ul>
    <% @pizza.errors.full_messages.each do |msg| %>
      <li><em><%= msg %><em></li></br>
    <% end %>
    </ul>
  </div>
<% end %>